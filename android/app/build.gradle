def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterRoot = localProperties.getProperty('flutter.sdk')
if (flutterRoot == null) {
    // throw new GradleException("Flutter SDK not found. Define location with flutter.sdk in the local.properties file.")
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}

apply plugin: 'com.android.application'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"
apply plugin: 'com.google.ar.sceneform.plugin'

android {
    compileSdkVersion 29
    compileOptions {
        sourceCompatibility 1.8
        targetCompatibility 1.8
    }
    sourceSets {
        main.java.srcDirs += 'src/main/java'
    }

    lintOptions {
        disable 'InvalidPackage'
    }

    defaultConfig {
        applicationId "com.example.pietrario_app"
        minSdkVersion 24
        targetSdkVersion 29
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.2.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.0.4'
    implementation 'com.google.ar.sceneform.ux:sceneform-ux:1.15.0'
}

sceneform.asset('sampledata/pietrario.obj',
        'default',
        'sampledata/pietrario.sfa',
        'src/main/res/raw/pietrario')

sceneform.asset('sampledata/guardian1.obj',
        'default',
        'sampledata/guardian1.sfa',
        'src/main/res/raw/guardian1')

sceneform.asset('sampledata/guardian2.obj',
        'default',
        'sampledata/guardian2.sfa',
        'src/main/res/raw/guardian2')

sceneform.asset('sampledata/succulent1.obj',
        'default',
        'sampledata/succulent1.sfa',
        'src/main/res/raw/succulent1')

sceneform.asset('sampledata/succulent2.obj',
        'default',
        'sampledata/succulent2.sfa',
        'src/main/res/raw/succulent2')

sceneform.asset('sampledata/succulent3.obj',
        'default',
        'sampledata/succulent3.sfa',
        'src/main/res/raw/succulent3')